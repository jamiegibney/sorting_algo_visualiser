use super::*;

#[derive(Debug)]
pub struct Comb {}

impl Comb {
    pub fn new() -> Self {
        Self {}
    }
}

impl SortAlgorithm for Comb {
    fn process(&mut self, arr: &mut SortArray) {
        //
    }
}
